{
  "name": "focus-widget",
  "version": "0.1.0",
  "private": true,
  "description": "A tiny desktop focus timer and daily task widget.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package:mac": "electron-packager . 好时光 --platform=darwin --arch=arm64 --out=dist --overwrite --asar=false --ignore=\"^/dist($|/)\" --ignore=\"^/openclaw-workspace($|/)\"",
    "package:win": "electron-packager . 好时光 --platform=win32 --arch=x64 --out=dist --overwrite --asar=false --ignore=\"^/dist($|/)\" --ignore=\"^/openclaw-workspace($|/)\""
  },
  "devDependencies": {
    "@electron/packager": "^20.0.0",
    "electron": "^37.2.3"
  }
}
